
%
%   First, install pkg:   tlmgr install ulem
%   2nd pandoc balks, b/c tex to pdf done by pdflatex
%   OK, pdflatex %  works !

%   pandoc % -t latex -o out.pdf 


\documentclass{article}
\usepackage{ulem}
\usepackage{xcolor}
\usepackage{amssymb}
%  check syntax - no errors = good?
\usepackage{syntonly}
%  \syntaxonly

\begin{document}

% in 1 line (flush left)
$\sum_{i=1}^n x_{i}$

% multiline (but no empty lines)
% both these are same
$$
\sum_{i=1}^n 
x_{i}
$$




$$ \sum_{i=2}^n x_{i} $$

$\frac{2}{3}$





\end{document}
